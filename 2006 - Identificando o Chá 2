#include <stdio.h>

int main() {
    int T, palpite;
    int acertos = 0;

    if (scanf("%d", &T) == EOF) return 0;

    for (int i = 0; i < 5; i++) {
        scanf("%d", &palpite);
        
        if (palpite == T) {
            acertos++;
        }
    }

    printf("%d\n", acertos);

    return 0;
}
